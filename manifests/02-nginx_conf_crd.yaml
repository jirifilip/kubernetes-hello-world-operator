apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition

metadata:
  name: nginxconfigs.jirifilip.github.com

spec:
  group: jirifilip.github.com

  names:
    kind: NginxConfig
    plural: nginxconfigs

  scope: Namespaced

  versions:
    - name: v1beta1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                pageContent:
                  type: string
